# REQUIRED - Sandboxes for code execution
# Get yours at https://e2b.dev
# sandbox is removed in this branch. local filesystem is used
# the name is project
# E2B_API_KEY=your_e2b_api_key_here

# NOT REQUIRED in this branch, not used - Web scraping for cloning websites
# Get yours at https://firecrawl.dev
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# OPTIONAL - AI Provider Selection
# Values: 'auto', 'anthropic', 'openai', 'google', 'groq', 'bedrock'
# 'auto' will use any available provider based on API keys
AI_PROVIDER=auto
NEXT_PUBLIC_AI_PROVIDER=auto # Set to 'bedrock' to show only Bedrock models in the UI

# OPTIONAL - App URL (used for server-to-server calls)
# Defaults to http://localhost:3000 if unset
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OPTIONAL - AI Providers (need at least one)
# Get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: override Anthropic base URL (enterprise/proxy setups)
ANTHROPIC_BASE_URL=https://api.anthropic.com/v1

# Get yours at https://platform.openai.com
OPENAI_API_KEY=your_openai_api_key_here

# Get yours at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Get yours at https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# OPTIONAL - Default model selection
# Used by endpoints when a model isn't specified in the request
# Choose from config `app/config.app.ts` appConfig.ai.availableModels
NEXT_PUBLIC_DEFAULT_MODEL=openai/gpt-4-turbo
# Legacy/generic fallback used by some endpoints
AI_MODEL=openai/gpt-4-turbo

# REQUIRED WHEN AI_PROVIDER=bedrock (or auto with AWS creds)
# AWS requires explicit model access; enforce via allowlist below.
# Option A: CSV of Bedrock modelIds
#   Example: BEDROCK_ALLOWED_MODELS="anthropic.claude-3-7-sonnet-20250219-v1:0,amazon.nova-pro-v1:0:24k"
BEDROCK_ALLOWED_MODELS=
# Client-side CSV (preferred for UI filtering)
#   Example: NEXT_PUBLIC_BEDROCK_ALLOWED_MODELS="anthropic.claude-3-7-sonnet-20250219-v1:0,amazon.nova-pro-v1:0:24k"
NEXT_PUBLIC_BEDROCK_ALLOWED_MODELS=
# Option B: Path to JSON file containing either:
#   - array of { modelId: "..." } or strings, or
#   - object with modelSummaries: [{ modelId: "..." }]
#   Example: BEDROCK_ALLOWED_MODELS_PATH=./mybedrockaccess.json
BEDROCK_ALLOWED_MODELS_PATH=

# AWS Bedrock Configuration
# Get your AWS credentials from https://console.aws.amazon.com/iam/home#/security_credentials
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-east-1  # Change to your preferred AWS region